# template-docker-local-run

First build your docker image:

```
make build
make rebuild # clean build push
make push
make pull
make clean

make debug # cmd as local user
make debug_root # cmd as root
```